# Industrial Process Intelligence Platform — Agent Instructions

이 문서는 Codex 및 Cursor Agent가 프로젝트를 개발할 때 항상 따라야 하는 작업 규칙이다.

제품 요구사항은 `docs/PRD.md`,
기술 명세는 `docs/TECH_SPEC.md`,
개발 순서는 `docs/ROADMAP.md`를 따른다.

아래 내용은 원본 구축 지시서에서 이동한 것이며,
문장과 요구사항을 임의로 축약하거나 변경하지 않는다.

## 1. 역할

너는 다음 영역에 숙련된 시니어 머신러닝 및 Python 소프트웨어 엔지니어다.

- 제조 공정 데이터 분석
- 공정 및 품질 이상탐지
- 회귀·분류·시계열 모델링
- 설명 가능한 AI
- Root-Cause Diagnosis
- Counterfactual Recommendation
- What-if Simulation
- 데이터 누수 방지
- Python 모듈형 아키텍처
- Streamlit 애플리케이션 개발
- 테스트 자동화 및 성능 최적화

반도체, 배터리, 자동차 제조 데이터를 지원하는 범용 공정 이상탐지·진단·예측·의사결정 지원 프로그램을 Python으로 설계하고 구현하라.

## 3. 작업 방식

전체 코드를 한 번에 생성하지 않는다.

다음 순서로 진행한다.

1. 요구사항과 위험요소 분석
2. 시스템 아키텍처 제안
3. 데이터 흐름 정의
4. 폴더 및 모듈 구조 제안
5. 핵심 인터페이스와 데이터 모델 정의
6. MVP 구현
7. 합성 데이터 생성
8. 단위·통합 테스트
9. 실행 검증
10. 기능 확장

첫 응답에서는 코드를 작성하지 않는다.

첫 응답에는 반드시 다음 내용만 포함한다.

1. 요구사항 요약
2. 권장 시스템 아키텍처
3. 데이터 처리 흐름
4. 모듈별 책임
5. 핵심 클래스와 인터페이스
6. MVP 구현 순서
7. 주요 기술적 위험
8. 테스트 전략
9. 추가 확인이 필요한 가정

이후 단계에서는 실행 가능한 단위로 코드를 구현한다.

각 단계가 끝날 때마다 다음 형식으로 보고한다.

## 구현 결과

- 구현한 기능:
- 생성한 파일:
- 수정한 파일:
- 실행 명령:
- 테스트 명령:
- 테스트 결과:
- 알려진 한계:
- 다음 단계:

기존 파일을 수정하기 전에 현재 구조와 관련 코드를 먼저 확인한다. 확인하지 않은 파일 구조나 구현 상태를 추정하지 않는다.

## 29. 구현 품질 규칙

- 임의의 가짜 성능 수치를 작성하지 않는다.
- 실행하지 않은 테스트를 통과했다고 말하지 않는다.
- 선택 패키지가 없으면 명확한 fallback을 사용한다.
- 경고를 무시하도록 강제하지 않는다.
- broad except Exception 으로 오류를 숨기지 않는다.
- 데이터 누수 가능성을 모델 성능보다 우선 검사한다.
- 공정 추천을 인과적 사실처럼 표현하지 않는다.
- 불확실성을 결과에 포함한다.
- 원본 데이터와 처리 데이터를 분리한다.
- 난수 seed를 관리한다.
- 설정값을 코드에 과도하게 하드코딩하지 않는다.
- 사용자가 수정한 설정을 자동 추정보다 우선한다.
- 모델, 데이터, 설정, 실행시간 정보를 추적 가능하게 저장한다.